24/09/24
Create a new db, name it "uvm_new". Export the "uvm_new.sql" file in the repo. Go to dbconnection.php, change the db name to "uvm_new". 
---------------------------------------------------------------------------------------------------------------------------------------

10/03/24
Add the new db "uvm_db". 
---------------------------------------------------------------------------------------------------------------------------------------
06/10/25
If error encountered finishing orders (QR generation). Go to XAMPP folder in your local disk, find the php folder. Inside the php folder search for php.ini. Finally, inside the notepad search for ";extensions = gd", then remove the semicolon. This allows the generation of qr images.
---------------------------------------------------------------------------------------------------------------------------------------
